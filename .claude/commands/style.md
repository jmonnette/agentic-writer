Run the Stylist agent to analyze /library and update the personal-voice skill.

Use incremental mode by default — analyze only new or modified files since the last style analysis run. If the user passes "full" as an argument, run a full re-analysis of the entire library from scratch.

Report what linguistic patterns were identified and how the voice profile was updated.
