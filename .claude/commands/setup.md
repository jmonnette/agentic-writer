Run the setup script to initialize the agentic writer environment:

```bash
./setup.sh
```

Report what directories and files were created, then remind the user of the next steps:
1. Add writing samples to /library
2. Run /archive to index them
3. Run /style to calibrate their voice
